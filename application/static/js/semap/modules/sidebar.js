export default function biuldSidebar(sector) {
  const sidebar = document.querySelector('[data-type="card"]');

  const single = document.querySelector('[data-card="single"]');
  const title = document.querySelector('[data-card="title"]');
  const biuld = document.querySelector('[data-card="title"]');
  const location = document.querySelector('[data-card="location"]');
}
